# Environment files — NEVER commit these
.env
.env.*
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
*.suo
*.user

# Obsidian vault — local notes only, not part of the codebase
second-brain/

# Node (if any root-level tooling)
node_modules/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Logs
*.log

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Secrets — belt-and-suspenders (gitleaks pre-commit is primary gate)
*.pem
*.key
secrets.*
credentials.*
